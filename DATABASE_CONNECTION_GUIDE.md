# 🔍 วิธีตรวจสอบและแก้ไข Database Connection

## ปัญหา: เพื่อนไม่เห็นหนังที่เพิ่มไป

### สาเหตุ:
ตอนนี้ **แต่ละเครื่องใช้ Database คนละตัว** (หรือเครื่องเพื่อนไม่มี database เลย)

```
เครื่องคุณ:     Backend → localhost:5433 (Database A)
เครื่องเพื่อน:   Backend → localhost:5433 (Database B หรือไม่มี!)
```

---

## 🛠️ วิธีแก้ไข (เลือก 1 วิธี)

### ✅ **Option 1: ใช้ Backend + Database เครื่องเดียว (แนะนำ)**

#### ขั้นตอนที่ 1: หา IP address ของเครื่องที่รัน Backend

**บนเครื่องที่รัน Backend:**
```powershell
# Windows
ipconfig | findstr IPv4

# ตัวอย่างผลลัพธ์:
# IPv4 Address. . . : 192.168.1.100
```

#### ขั้นตอนที่ 2: เปลี่ยน Frontend ให้เชื่อมต่อกับ Backend บนเครื่องคุณ

**บนเครื่องเพื่อน** - แก้ไขทุกไฟล์ที่มี `http://localhost:3000`:

**ไฟล์ที่ต้องแก้:**
- `frontend/src/App.tsx`
- `frontend/src/pages/Home.tsx`
- `frontend/src/pages/AddMoviePage.tsx`
- `frontend/src/pages/EditMoviePage.tsx`
- `frontend/src/pages/MovieDetail.tsx`
- `frontend/src/components/LoginForm.tsx`

**เปลี่ยนจาก:**
```typescript
fetch('http://localhost:3000/movies')
```

**เป็น:**
```typescript
fetch('http://192.168.1.100:3000/movies')  // ใช้ IP ของเครื่องคุณ
```

#### ขั้นตอนที่ 3: เปิด Firewall (ถ้าจำเป็น)

**บนเครื่องที่รัน Backend:**
```powershell
# อนุญาต port 3000
New-NetFirewallRule -DisplayName "Backend API" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

---

### ✅ **Option 2: สร้าง .env และใช้ Environment Variable**

สร้างไฟล์ `.env` ใน `frontend/`:

```env
# เครื่องตัวเอง
VITE_API_URL=http://localhost:3000

# เครื่องเพื่อน (ใช้ IP ของเครื่องที่รัน Backend)
VITE_API_URL=http://192.168.1.100:3000
```

แล้วใช้ในโค้ด:
```typescript
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000';
fetch(`${API_URL}/movies`)
```

---

### ✅ **Option 3: Deploy Database ให้เข้าถึงได้จากภายนอก**

ถ้าต้องการให้แต่ละเครื่องรัน Backend เอง แต่ใช้ Database ร่วมกัน:

#### แก้ `docker-compose.yml`:
```yaml
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: movie_review_db
    ports:
      - '5433:5432'
    # เพิ่ม network binding
    command: postgres -c listen_addresses='*'
```

#### เพิ่มใน `backend/src/app.module.ts`:
```typescript
TypeOrmModule.forRoot({
  type: 'postgres',
  host: '192.168.1.100',  // IP ของเครื่องที่รัน Database
  port: 5433,
  username: 'admin',
  password: 'password123',
  database: 'movie_review_db',
  entities: [User, Movie, Review, Genre],
  synchronize: true,
})
```

---

## 🧪 วิธีทดสอบว่าเชื่อมต่อได้

### 1. ตรวจสอบ Backend:
```bash
# จากเครื่องเพื่อน
curl http://192.168.1.100:3000/
# ควรได้ response: {"status": "OK", "message": "Movie Review API is running! 🎬"}
```

### 2. ตรวจสอบ Database:
```bash
# จากเครื่องที่รัน Backend
docker ps
# ควรเห็น postgres container running
```

### 3. ตรวจสอบข้อมูลใน Database:
```bash
# เข้า Docker container
docker exec -it <container_id> psql -U admin -d movie_review_db

# Query ดูหนัง
SELECT movie_id, title FROM movie;
```

---

## 📝 สรุป

**เหตุผลที่เพื่อนไม่เห็นหนัง:**
- แต่ละเครื่องต่อ database คนละตัว
- Backend บันทึกข้อมูลลงใน database บนเครื่องของคุณ
- เครื่องเพื่อนดึงข้อมูลจาก database บนเครื่องของเขา (ที่ไม่มีข้อมูล)

**วิธีแก้ที่เร็วที่สุด:**
ให้เพื่อนแก้ URL ทั้งหมดใน frontend จาก `localhost:3000` เป็น IP เครื่องคุณ (เช่น `192.168.1.100:3000`)
