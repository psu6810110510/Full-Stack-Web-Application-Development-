# 👥 สำหรับทีมงาน/เพื่อนที่จะเข้าร่วมโปรเจค

## 🎯 แก้ปัญหา "ไม่เห็นข้อมูลเดียวกัน"

### วิธีที่ 1: ใช้ Docker (แนะนำ ⭐)

**ข้อดี:**
- รันคำสั่งเดียวได้ทุกอย่าง
- ทุกคนใช้ database เดียวกัน
- ไม่ต้องติดตั้งอะไรเลยนอกจาก Docker

**ขั้นตอน:**
```bash
# 1. Clone โปรเจค
git clone <repo-url>
cd mini-project

# 2. รัน Docker
docker-compose up -d

# 3. เข้าใช้งาน
# เปิด browser: http://localhost:5173
```

✅ เสร็จแล้ว! จะเห็นข้อมูลเดียวกับทุกคน

---

### วิธีที่ 2: เชื่อมต่อกับเครื่องคนอื่น

ถ้าไม่ต้องการติดตั้ง Docker:

**ให้คนที่มี Docker รัน:**
```bash
docker-compose up -d
```

**คนอื่นๆ ทำ:**

1. ถาม IP ของเครื่องที่รัน Docker (เช่น `192.168.1.100`)

2. สร้างไฟล์ `frontend/.env`:
```env
VITE_API_URL=http://192.168.1.100:3000
```

3. รัน frontend:
```bash
cd frontend
npm install
npm run dev
```

4. เปิด browser: `http://localhost:5173`

✅ จะเห็นข้อมูลเดียวกัน!

---

## 🔐 Login Credentials

### Admin (สามารถเพิ่ม/ลบหนัง)
- Username: `admin`
- Password: `admin123`

### User ทั่วไป
- Username: `user1`
- Password: `password123`

---

## 🐛 พบปัญหา?

### ❌ Cannot connect to backend
```bash
# ตรวจสอบว่า backend รันอยู่
docker-compose ps

# ดู logs
docker-compose logs backend
```

### ❌ Cannot access from another machine
1. ตรวจสอบ Firewall บนเครื่องที่รัน Docker
2. ให้แน่ใจว่าอยู่ในเครือข่ายเดียวกัน
3. ลอง ping IP ของเครื่องที่รัน Docker

---

## 📖 อ่านเพิ่มเติม

- [DOCKER_SETUP_GUIDE.md](DOCKER_SETUP_GUIDE.md) - คู่มือ Docker แบบละเอียด
- [DATABASE_CONNECTION_GUIDE.md](DATABASE_CONNECTION_GUIDE.md) - คู่มือแก้ปัญหา database connection
